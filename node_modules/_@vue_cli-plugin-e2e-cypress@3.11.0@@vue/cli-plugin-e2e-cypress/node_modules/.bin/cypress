#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_cypress@3.4.1@cypress/bin/cypress" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_cypress@3.4.1@cypress/bin/cypress" "$@"
  ret=$?
fi
exit $ret
